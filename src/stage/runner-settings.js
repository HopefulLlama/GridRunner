class RunnerSettings {
	constructor(maxRunners, size, speed, trailLength, initialTrailOpacity) {
		this.maxRunners = maxRunners;
		this.size = size;
		this.speed = speed;
		this.trailLength = trailLength;
		this.initialTrailOpacity = initialTrailOpacity;
	}
}